function setup() {
    createCanvas (2000, 1000);

}

function draw() {
    if (mouseIsPressed) {
        fill(226, 209, 249);
        ellipse(mouseX, mouseY, 80, 80);
      } else {
        
      }
}